---
"tree-sitter-marko": minor
---

Support the `async` shorthand method modifier as an `attr_method_async` node, eg `<button async onClick() { await save() }>`, keep a whitespace preceded `>=` in an attribute value instead of ending the tag, and stop an unenclosed attribute value from swallowing a following close tag.
