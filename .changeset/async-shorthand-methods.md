---
"tree-sitter-marko": minor
---

Support the `async` shorthand method modifier as an `attr_method_async` node, eg `<button async onClick() { await save() }>`, and keep a whitespace preceded `>=` in an attribute value instead of ending the tag.
